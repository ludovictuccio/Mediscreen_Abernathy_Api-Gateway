server:
  port: 8080
 
spring:
  application:
    name: api-gateway  
  cloud:
    gateway:     
      routes:
      - id: microservice-patient
        uri: "${proxy.patient}"
        predicates:
        - Path=/api/patient/**
 
         
logging:
  level:
     '[com.mediscreen.apigateway]': DEBUG